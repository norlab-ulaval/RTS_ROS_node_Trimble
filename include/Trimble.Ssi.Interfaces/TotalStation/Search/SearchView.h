#ifndef SEARCHVIEW_H_
#define SEARCHVIEW_H_

namespace SSI
{
	/**
	* Represents the view of the multi target search.
	*/
	enum SearchView
	{
		SV_WideView, /**< Do the search with a wide view angle. */
		SV_SmallView /**< Do the search with a small view angle. */
	};
}
#endif